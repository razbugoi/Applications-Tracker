import { StatusPage } from '@/components/StatusPage';

export default function InvalidatedPage() {
  return (
    <StatusPage status="Invalidated" title="Invalidated Applications" subtitle="Applications with outstanding issues to resolve." />
  );
}
